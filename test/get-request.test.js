var
  chai = require('chai'),
  expect = chai.expect,
  sinon = require('sinon'),
  mockery = require('mockery');

chai.use(require("sinon-chai"));

describe('GET request', function() {

  // TODO unit test a Shopping service request ...
  // stub out `require.get`, make sure it's getting passed the expected options!

  // then try to hit an actual sandbox endpoint ...?
  // (can a sandbox key be hard-coded in this module?)

});